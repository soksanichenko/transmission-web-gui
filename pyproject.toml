[project]
name = "transmission-web-gui"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cryptography==50.0.1",
    "fastapi==0.141.1",
    "uvicorn[standard]==0.52.4",
]

[tool.setuptools.packages.find]
where = ["backend"]

[tool.ruff.format]
quote-style = "single"
