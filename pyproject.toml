[project]
name = "transmission-web-gui"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.139.2",
    "uvicorn[standard]==0.51.0",
]

[tool.setuptools.packages.find]
where = ["backend"]

[tool.ruff.format]
quote-style = "single"
