[project]
name = "transmission-web-gui"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.6",
    "uvicorn[standard]==0.49.0",
]

[tool.setuptools.packages.find]
where = ["backend"]

[tool.ruff.format]
quote-style = "single"
